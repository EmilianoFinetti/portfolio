version: '3.8'

services:
  portfolio:
    image: nginx:alpine
    container_name: emiliano-finetti
    ports:
      - "8080:80"
    volumes:
      - ./public:/usr/share/nginx/html:ro
      - ./conf:/etc/nginx/conf.d:ro
    restart: unless-stopped
